# invalid: archive_path names a member of a downloaded archive, and there is
# nothing here to download.
version: 1
modules:
  - path: proto
generate:
  - name: dart
    inputs:
      - module: proto
    plugins:
      - local: protoc-gen-dart
        archive_path: bin/protoc-gen-dart
        out: lib/gen
