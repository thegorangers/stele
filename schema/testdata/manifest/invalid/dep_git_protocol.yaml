# invalid: the git:// protocol is unauthenticated and unencrypted, and major
# hosts have disabled it; use https:// or ssh://.
version: 1
deps:
  - name: example
    git: git://git.example.com/acme/example.git
    ref: v1.0.0
