# beyond: the id is shaped correctly, but no rule this tool has carries it.
# The set of rules the tool ships with is a fact about the binary, not about
# this file, so the schema cannot check it.
version: 1
modules:
  - path: api
breaking:
  rules:
    - id: break/no_such_rule
      severity: error
