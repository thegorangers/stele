version: 1
modules:
  - path: api
lint:
  ignore:
    - api/third_party
  rules:
    - id: stele/enum_value_prefix
      severity: warning
      ignore:
        - api/example/legacy
    - id: stele/package_version_suffix
      severity: "off"
