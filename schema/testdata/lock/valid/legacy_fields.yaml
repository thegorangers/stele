# A lock written before the per-file hashes were dropped. It is still read, so
# the schema still describes those fields, marked deprecated.
version: 1
deps:
  - name: example
    git: gh:acme/example
    ref: v1.4.0
    sha: 9f4c2b1a5d7e3f6081b2c4d5e6f708192a3b4c5d
    modules:
      - proto
    manifest: 4f9a1c
    files:
      acme/example/v1/example.proto: 2b7d0e
