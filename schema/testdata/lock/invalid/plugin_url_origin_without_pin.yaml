# invalid: a record that claims the download tier without both halves of the
# pin claims a pin it does not have.
version: 1
plugins:
  - name: protoc-gen-dart
    origin: url
    os: linux
    arch: amd64
    url: https://example.com/protoc-gen-dart
    version: unknown
